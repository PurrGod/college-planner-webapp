import "./App.css";
import Bar from "./bar"; // Assuming bar.js is in the same directory
import Calendar from "./calendar"; // Assuming calendar.js is in the same directory

function App() {
  return (
    <div className="App">
      <Bar />
      <Calendar />
    </div>
  );
}

export default App;
